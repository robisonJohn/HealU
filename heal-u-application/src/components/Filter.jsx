import "Feeling";

// the purpose of filter is to sort items by date and emotion
// Ideally, when we enter a date into one input and an emotion into the second input, we will be 
// able to filter elements that only contain this particular date and emotion
function Filter() {

    return (
        <h1>Sample Return</h1>
    )
}
export default Filter;